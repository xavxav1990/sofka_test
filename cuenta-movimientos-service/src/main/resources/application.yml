

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cuenta_movimientos_db
    username: xloaiza
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest



server:
  port: 8080
  error:
    include-message: always
    
logging:
  level:
    root: DEBUG

